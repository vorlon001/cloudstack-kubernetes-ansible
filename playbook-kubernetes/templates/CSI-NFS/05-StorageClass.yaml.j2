---
apiVersion: storage.k8s.io/v1
# https://kubernetes.io/docs/concepts/storage/storage-classes/
kind: StorageClass
metadata:
  name: managed-nfs-storage
  namespace: "{{CSI.NFS.NAMESPACE}}"
provisioner: "{{CSI.NFS.PROVISIONER_NAME}}"
parameters:
  archiveOnDelete: "false"
